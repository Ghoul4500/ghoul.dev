---
import { education, otherAwards } from '../lib/clients.ts';
---

<section id="education" class="relative py-32 md:py-48 px-6 md:px-10 border-t hairline">
  <div class="mx-auto w-full max-w-[1400px]">
    <div class="grid md:grid-cols-[0.4fr_1.6fr] gap-10 md:gap-20">
      <div class="reveal">
        <span class="label">
          [ 07 · education & awards ]
        </span>
      </div>

      <div>
        <h2 class="font-display text-3xl md:text-5xl font-semibold tracking-[-0.01em] leading-[1.1] reveal">
          School and <span class="text-[var(--color-ink-dim)]">a few medals.</span>
        </h2>

        <ol class="mt-12 divide-y divide-white/[0.06]">
          {education.map((e) => (
            <li class="reveal py-8">
              <div class="flex items-baseline justify-between gap-4">
                <h3 class="font-display text-base md:text-lg font-semibold tracking-[-0.005em]">
                  {e.qualification}
                </h3>
                <span class="font-mono text-[10px] uppercase tracking-[0.22em] text-[var(--color-ink-faint)] whitespace-nowrap">
                  {e.period}
                </span>
              </div>
              <div class="mt-1 font-mono text-[12px] text-[var(--color-ink-dim)]">
                {e.school}
                {e.partnerSchool && (
                  <>
                    <span class="text-[var(--color-ink-faint)] mx-1.5">·</span>
                    <span>{e.partnerSchool}</span>
                  </>
                )}
              </div>

              {e.awards && e.awards.length > 0 && (
                <ul class="mt-5 grid grid-cols-1 sm:grid-cols-2 gap-2">
                  {e.awards.map((a) => (
                    <li class="flex items-baseline gap-2.5 rounded-lg border border-white/[0.05] bg-white/[0.015] px-3.5 py-2.5 ring-line hover:border-white/15 transition-colors">
                      <span class="text-[var(--color-acid)] flex-none">✦</span>
                      <span class="font-display text-[13.5px] font-semibold tracking-[-0.005em] text-[var(--color-ink)]">
                        {a}
                      </span>
                    </li>
                  ))}
                </ul>
              )}
            </li>
          ))}
        </ol>

        {otherAwards.length > 0 && (
          <div class="reveal mt-10 pt-8 border-t border-white/[0.06]">
            <h3 class="font-display text-base md:text-lg font-semibold tracking-[-0.005em] mb-5">
              Other distinctions
            </h3>
            <ul class="grid grid-cols-1 sm:grid-cols-2 gap-2">
              {otherAwards.map((a) => (
                <li class="flex flex-wrap items-baseline justify-between gap-x-3 gap-y-1.5 rounded-lg border border-white/[0.05] bg-white/[0.015] px-3.5 py-2.5 ring-line hover:border-white/15 transition-colors">
                  <span class="font-display text-[13.5px] font-semibold tracking-[-0.005em] text-[var(--color-ink)]">
                    <span class="text-[var(--color-acid)] mr-2">✦</span>{a.title}
                  </span>
                  <span class="font-mono text-[10px] uppercase tracking-[0.2em] text-[var(--color-ink-faint)] whitespace-nowrap">
                    {a.where} · {a.year}
                  </span>
                </li>
              ))}
            </ul>
          </div>
        )}
      </div>
    </div>
  </div>
</section>
